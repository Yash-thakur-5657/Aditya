# Only needed if the frontend is deployed on a different origin than the backend
# (e.g. frontend on Vercel/Netlify, backend elsewhere). Leave unset when using
# docker-compose / the bundled nginx, which proxies /api and /test-widget same-origin.
# VITE_API_BASE_URL=https://your-backend-domain.com
